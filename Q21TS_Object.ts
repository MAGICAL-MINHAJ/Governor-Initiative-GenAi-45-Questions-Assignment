// object about vehicle.
let vehicle: { type: string, brand: string, model: string, year: number } = {
    type: "Car",
    brand: "Nissan",
    model: "GTR",
    year: 1998
};

// info of vehicle.
console.log("Vehicle:", vehicle);